"""UI module for LKS Keymap Conflict Resolver (Dev).

The conflict-resolver UI has moved to addon preferences
(Edit → Preferences → Add-ons). No sidebar panel is registered.
"""

from __future__ import annotations
